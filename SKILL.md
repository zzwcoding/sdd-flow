---
name: sdd-flow
description: 编排型工作手册——从 PRD 到交付的 SDD 全流程：需求逼问、深模块划分、功能规格、拆票、TDD 实现、CI 守门、定期架构体检。含窗口分层协议（总窗口统筹、中层窗口设计、编码窗口执行）。当用户要求"按流程开发新功能""从 PRD 开始规划""继续按文档实施任务""搭 CI"或提到 sdd-flow 时使用。本 skill 是路由器和产物契约，具体纪律由它调用的底层 skill 与 references 文档承担。
---

# sdd-flow：SDD 组合流程手册

这是一本**指导手册**，不是执行脚本。你的工作是：判断项目当前处于哪个阶段，把用户带到正确的阶段路线，调用对应的底层 skill，并保证每个阶段的**产物**按约定落盘。真正的纪律（逼问、TDD、架构评审）在底层 skill 里；设计判断的标准在 `references/` 里，**进入对应阶段时先读对应的 reference 文档**。

## 五条核心原则（不可妥协）

1. **接口先行**：任何功能，先定模块边界和接口（输入输出），再写实现。接口由人拍板，实现委托给 AI。
2. **测试锁行为**：每个模块的行为测试写在 seam（接缝）处，只测公开接口，不测内部实现。测试通过 = 模块完成。
3. **产物即状态**：项目进展不靠聊天记录，靠落盘的产物。新窗口通过读产物恢复上下文。
4. **CI 是守门员**：任何代码进主干前必须过 CI（lint + 类型 + 测试）。CI 不绿不合并。
5. **窗口分层**：统筹、设计、编码分别在三层窗口进行，靠交接包衔接（见下）。

## 窗口分层与交接协议

整个项目由三层对话窗口协同，每层职责单一：

- **L0 总窗口（统筹层）**：阶段路由、跨模块决策、产物验收、定期体检、向用户汇报全局进展。总窗口**不直接写实现代码**，也不展开打磨单个模块的设计细节。
- **L1 中层窗口（设计层）**：一次只打磨一个中层任务——某模块的接口设计、某功能的 spec、某项技术决策。产物是定稿的文档。
- **L2 执行窗口（编码层）**：spec 与细则全部定稿后的编码实现。

规则：

1. 任何一层识别出"这是一个需要多轮专注交互的子任务"，**不要就地展开**——产出交接包，请用户开新窗口执行，然后停下等汇报。
2. 编码类任务一律走 L2，按交接包的 `模式` 分两档。**互动档**以 `learn-by-rebuild` 的纪律为主框架：开工先给完整阶段路线（简单→复杂，用户确认后执行）、小步增量（每阶段 ≤30 行新增、有可观察变化）、讲解落盘 `lessons/`、用户说"下一步"才推进、说"提交"才 commit。**自主档**不装 `learn-by-rebuild`：spec 已定稿、验收可测、CI 存在，主代理**逐张派发、默认不并行**（要并行需用户明确开口），子代理自驱 `tdd` 循环（测试先行、测试写 seam 处）直到 CI 绿，中途不停下等确认，完成后同样落盘 `lessons/` 并跑 `code-review`。
3. 下层完成后用户回上层汇报；上层核对产物、更新状态（spec 状态、票状态、wayfinder map），再路由下一步。
4. 交接发现细则没定全（接口含糊、验收不可测）→ 不发交接包，先在当前层补齐。
5. 交接包的 `先读` 清单必须最小化：只列本任务直接依赖的产物文件（可精确到节），禁止整个目录或整份长文档；执行中发现缺文档，回报总窗口补发，不自行全仓扫读。

交接包模板（总窗口/中层窗口发给用户，用户原样粘进新窗口）：

```
任务: <一句话目标>
角色: L1 设计 / L2 编码
模式: 自主 / 互动（L2 专用。判定：spec 已定稿 + 验收标准逐条可测 + CI 存在 → 自主，否则互动）
项目: <仓库根路径>
先读: <产物路径清单，如 specs/modules.md 第 X 节、CONTEXT.md>
调用: <skill 名，如 tdd / learn-by-rebuild / grill-with-docs>
产出: <产物路径>
验收: <可判断真伪的通过标准>
回报: 完成后回总窗口说"<任务名>完成，产物在 <路径>，偏差: <有/无>"
```

## 新窗口入口协议

任何窗口被调用时，先定位再行动：

1. 确认自己身处哪一层（用户带了交接包 → 按交接包角色；没有 → 默认 L0 总窗口）。
2. 读仓库根目录的 `CONTEXT.md`（不存在 → 阶段 0/1）。
3. 看 `specs/` 目录：`modules.md` 是否存在（模块划分是否完成）、有哪些功能 spec、各自状态。
4. 看 issue tracker（配置见 `docs/agents/` 或 `.scratch/`）：有没有进行中的票。
5. 向用户报告判断："当前处于阶段 X，产物 Y 已有/缺失"，给出下一步路线，**然后停下等用户拍板再行动**。**不要跳过阶段**——缺什么产物就回哪个阶段补。

## 产物总览与路径约定

| 产物 | 路径 | 产生阶段 |
|---|---|---|
| 产品需求文档 | `docs/prd.md` | 输入粗稿，阶段 1 逼问定稿，决策回写 |
| 共享术语表 | `CONTEXT.md` | 1，持续更新 |
| 架构决策记录 | `docs/adr/*.md` | 任何阶段，决策时 |
| 架构图（archify HTML） | `docs/architecture-v*.html` | 2，编码前必须过审 |
| 模块划分图 | `specs/modules.md` | 2 |
| 功能规格 | `specs/<功能名>.md` | 3 |
| 任务票 | issue tracker | 4 |
| 阶段讲解 | `lessons/NNNN-*.md` | 5（learn-by-rebuild 纪律） |
| CI 配置 | `.github/workflows/ci.yml` | 6（尽早） |

目录细节见 `references/repo-layout.md`。

### 状态流转（谁在什么事件翻转）

产物状态是路由依据，翻转必须与事件同步，不许事后补账：

| 状态 | 翻转时机 | 谁来翻 |
|---|---|---|
| PRD：粗稿 → 逼问中 → 已定稿 | 阶段 1 逼问收敛：清单 11 维逐维关闭、无残留 `[待确认]`，每个功能能一句话说清"做什么、不做什么" | 总窗口 |
| spec：草稿 → 已定稿 | 阶段 3 通过标准达成（每条验收标准对应一个可执行测试），且无未解决项 | 总窗口 |
| spec：已定稿 → 实现中 | 对应票的 L2 窗口开工时 | L2 |
| spec：实现中 → 已完成 | CI 绿 + code-review 通过 + 收尾回写齐全 + **名下所有票均 `done`** | 总窗口 |
| 票：完成 | 同上，L0 核对后关闭（本地票置 `Status: done`，GitHub 关 issue） | 总窗口 |
| 架构图：过审 | 用户逐节点过完图并明确认可 | 当场记 ADR |

## 阶段路由

### 阶段 0：项目初始化（每仓库一次）

- 调用 `setup-matt-pocock-skills`，确定 issue tracker（GitHub 或本地 markdown）、文档位置。若仓库还没有 CI，直接进入阶段 6 把 CI 先搭起来——**CI 要在第一行业务代码之前存在**，并把 spec gate 一并接上（`tools/check_specs.py`，来源 `references/spec-validator.py`；specs/ 尚不存在时自动跳过）。技术栈在此宣布即可，不追求一次到位：阶段 1 逼问若推出与栈冲突的非功能需求，记 ADR 换栈并回本阶段改 CI（见失败处理）。

### 阶段 1：需求对齐

目标：把 PRD/想法逼问成无歧义的需求，同时建立共享术语。

- **先读 `references/requirement-checklist.md`**——11 维逼问覆盖清单：A/B 分流、固定提问格式、逐维关闭、`[待确认]` 硬门禁。
- 调用 `grill-with-docs`（它会驱动逼问、更新 `CONTEXT.md`、必要时记 ADR）。粗糙 PRD 若只存在于聊天记录，先落盘为 `docs/prd.md` 再开始逼问。
- 产出：`docs/prd.md` 定稿（顶部 `状态: 粗稿 | 逼问中 | 已定稿`；决策当场回写，含变更记录）+ `CONTEXT.md`。
- 通过标准：每个功能能用 `CONTEXT.md` 里的术语一句话说清"做什么、不做什么"；清单 11 维逐维标注 适用 / N/A（含理由），PRD 内无残留 `[待确认]`。

### 阶段 2：模块划分（本项目最关键的阶段）

目标：把系统拆成深模块，定死边界和接口，并让**用户在架构图上看到全系统**。

- **先读 `references/deep-modules.md`**，用它里面的标准做判断。
- 调用 `codebase-design` 获取深模块设计纪律；对每个拿不准的边界，用 `grilling` 和用户过一遍。模块多、讨论量大时，每个模块的接口打磨发 L1 中层窗口做。
- 产出 1：`specs/modules.md`——**聚类一拍板即落骨架**（每模块一节占位），随后逐卡填充：职责一句话、公开接口（输入输出签名）、依赖哪些模块（`- 模块:` 反引号行）、seam 在哪、测试计划。**卡是唯一真源**。
- 产出 2：**架构图**（用 `archify`，有前序版本则以其为底更新）：完整展示组件、模块边界、关键消息流、安全防线落位。图是用户的掌控工具，不是装饰，也是卡的可视投影：**阶段 2 内每次卡变更后重出图再续导览**；过审进入编码期后，图不强制随卡同步（编码以卡为准），阶段 7 体检时一并刷新。
- 产出 3：**过审记录**——用户过完图当场记一条 ADR（`docs/adr/NNNN-架构图v<X>过审.md`）：日期、结论、用户提出的修改及落点、遗留问题。没有过审记录 = 没有过审。
- 过审落锤后：立即按 `specs/modules.md` 建目录骨架——每模块一个目录 + 占位 `index.*`。文件系统 = 模块结构从此成立，spec gate 的目录警告随之消失。
- 验收方式：**先读 `references/guided-review.md`**——导览式验收：架构图节点可点击跳详情页，带用户一次一个节点过图，大白话讲解，不催进度，用户提问产生的决策当场四处落盘（PRD/模块卡/术语表/注解）。
- 通过标准：① 用户逐节点过完架构图；② 模块间调用都走在接口上，依赖无环，没有浅模块；③ **架构图经用户过审**——图没过审，任何编码不许开始。

### 阶段 3：功能规格

目标：每个功能一份可验收的 spec。

- **先读 `references/module-spec-format.md`**，spec 必须包含其中定义的章节（尤其：触及哪些模块、接口变化、验收测试）。
- **起草顺序：主干链路优先**——先立"系统存在目的"那条主路径的 spec，装饰性功能靠后：主功能的接口最可能牵动 modules.md 卡，改卡要趁早。
- 已有充分讨论时调用 `to-spec` 直接合成；讨论不足先回阶段 1 补逼问。
- 产出：`specs/<功能名>.md` + tracker 上的对应 issue。
- 通过标准：spec 里的每条验收标准都对应一个可执行的测试；`python3 tools/check_specs.py` 通过（机器校验先于 L0 人审，格式契约见 module-spec-format.md「机器可校验约定」）。

### 阶段 4：拆票

- 普通功能：调用 `to-tickets`，把 spec 拆成带依赖关系的任务票，每张票一次会话能完成。
- 超出单次会话的大块工作：调用 `wayfinder` 建决策地图。
- 通过标准：每张票声明了触及的模块和 blocking 边；没有一张票需要跨多个模块改接口（有就说明阶段 2 的划分要修）。本地票带 `Touches modules:` 行（反引号模块名，取自 modules.md），spec gate 会校验。**票的每条验收条目注明源自 spec 验收测试表的哪一行**——5.4 验收时 L0 按此对账。

### 阶段 5：实现（走 L2 编码窗口）

- 总窗口不编码。票对应的 spec 与细则定稿 → 总窗口发交接包（角色 L2，按判定规则填 `模式`：自主 / 互动）→ 用户开新窗口执行 → 完成后回总窗口汇报。
- L2 窗口内：互动档先出阶段路线（每阶段小步、有可观察变化），逐阶段推进；自主档直接按 spec 跑 `tdd` 循环。两者共同点：编码遵守 `tdd`（测试先于实现、写在 seam 处）；收尾跑 `code-review`；讲解落盘 `lessons/`。
- 铁律：实现只能在模块内部活动，**不许顺手改别的模块的接口**——需要改接口就停下来，回 L0 总窗口升级处理（回阶段 2/3）。
- 收尾（文档回写，L0 核对后才接受回报）：L2 完成时回写 ① `specs/<功能名>.md` 中与实现有出入的行为约定（状态交 L0 翻转）② `specs/modules.md`（同步实现事实；涉及接口变更的不许就地改，走铁律回阶段 2）③ `CONTEXT.md`（新增/变更术语）④ `lessons/`（自主档定位为**施工日志**：这票做了什么、踩了什么坑、关键取舍——它是自主档唯一的过程记录，服务中断恢复与阶段 7 体检）。L0 核对四样齐全后翻转 spec 状态、关票、路由下一步。
- 通过标准：CI 绿 + code-review 通过 + 验收标准逐条有测试对应 + 收尾回写完成。

### 阶段 6：CI 守门

- **先读 `references/ci-template.md`**，按技术栈实例化 `.github/workflows/ci.yml`。
- 最小门禁：spec gate + lint + 类型检查 + 全部测试。有模块边界检查工具（dependency-cruiser / import-linter）就加上，禁止跨模块乱 import。
- 这是阶段 0 就该存在的东西，这里是补建和维护的入口。

### 阶段 7：架构体检（定期，建议每几天一次）

- 调用 `improve-codebase-architecture`，扫浅模块、并行重复实现、没测试的 seam。
- 产出候选清单，**由用户拍板**哪个要做，选中的转成 issue 回到阶段 4。

## 失败处理

- 实现中发现 spec 有漏洞 → 回阶段 3 补 spec，不要在代码里猜。
- 发现模块边界错了 → 回阶段 2，改 `specs/modules.md` 并记 ADR。
- 阶段 1 逼问出的非功能需求（量级/并发/合规）与阶段 0 宣布的技术栈冲突 → 记一条 ADR 说明换栈理由，回阶段 0 改 CI，再继续逼问。
- **回退到某阶段时，该阶段产物的状态同步回退**：回阶段 1 → PRD 翻回"逼问中"、新疑问以 `[待确认]` 标注；回阶段 3 → spec 回"草稿"。清零后重新定稿。
- 下层窗口回报偏差 → 上层先核对产物再决定：接受（更新文档）或返工（重开交接包）。
- 底层 skill 缺失（新机器）→ 提示用户从 mattpocock/skills 安装：`npx skills@latest add mattpocock/skills`。

## References

- `references/requirement-checklist.md` — 需求逼问 11 维覆盖清单（阶段 1 必读）
- `references/spec-validator.py` — spec gate 校验脚本，复制到仓库 `tools/check_specs.py`（阶段 0/3/4/6）
- `references/deep-modules.md` — 深模块划分准则（阶段 2 必读）
- `references/module-spec-format.md` — 功能 spec 与模块接口卡模板（阶段 3 必读）
- `references/ci-template.md` — CI 流水线骨架（阶段 6 必读）
- `references/repo-layout.md` — 仓库目录约定（阶段 0/2 参考）
- `references/guided-review.md` — 导览式验收：带用户逐节点过架构图（阶段 2 验收必读）
